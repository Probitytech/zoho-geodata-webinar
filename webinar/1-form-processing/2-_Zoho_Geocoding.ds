string _Zoho_Geocoding()
{
	rec = Apartment[ID != 0] range from 1 to 100;
	counter = 0;
	for each  r in rec
	{

		response = zoho.map.geoCode(r.Address.toString());
		if(response.get("status") == "success")
		{
			counter = counter + 1;
		}
	}
	return "Geocoded: "+ counter;
}